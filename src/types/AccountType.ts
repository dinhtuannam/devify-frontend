export interface currentUserInformation {
    Id: string;
    UserName: string;
    Email: string;
    RoleId: string;
}

export interface currentUserType {
    id: string;
    username: string;
    email: string;
}
