export interface accountInformation {
    Id: string;
    UserName: string;
    Email: string;
    RoleId: string;
}

export interface currentUserType {
    id: string;
    username: string;
    email: string;
}
